# lambda-sns-notification-project
This project integrates AWS Lambda with Amazon SNS for notifications, using EventBridge cron expressions to trigger the Lambda function on a schedule. The repository includes the Lambda code, an IAM policy (in a separate JSON file) for SNS publishing, and EventBridge cron scheduling instructions.
